"""
Consensus — LangGraph Agent Service (stub)
Implementation blocked until Phase 1 graph design is approved.
"""
import logging

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

if __name__ == "__main__":
    logger.info("Consensus agent service — awaiting Phase 1 graph design approval.")
